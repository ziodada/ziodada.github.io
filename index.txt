<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scegli il tuo Regalo di Natale</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(to bottom, #ffe6e6, #e6ffe6);
        }
        .container {
            text-align: center;
            background-color: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #d32f2f;
        }
        .gift-icon {
            font-size: 5rem;
            color: #4caf50;
            margin: 1rem 0;
        }
        button {
            background-color: #d32f2f;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #b71c1c;
        }
        .result {
            margin-top: 1rem;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Scegli il tuo Regalo di Natale!</h1>
        <div id="giftPicker">
            <button onclick="pickGift()">Scegli un Regalo</button>
        </div>
        <div id="giftReveal" style="display: none;">
            <div class="gift-icon">🎁</div>
            <p>Hai scelto un regalo! Vuoi scoprire cosa hai vinto?</p>
            <button onclick="revealGift()">Rivela il Regalo</button>
        </div>
        <div id="giftResult" style="display: none;">
            <p class="result">Congratulazioni! Hai vinto: <span id="prize"></span></p>
            <button onclick="reset()">Gioca di Nuovo</button>
        </div>
    </div>

    <script>
        const prizes = [
            { name: "Sconto del 10%", probability: 0.4 },
            { name: "Sessione gratuita", probability: 0.3 },
            { name: "Buono regalo da 50€", probability: 0.2 },
            { name: "Pacchetto Deluxe", probability: 0.1 }
        ];

        let selectedPrize;

        function pickGift() {
            const random = Math.random();
            let cumulativeProbability = 0;
            
            for (let prize of prizes) {
                cumulativeProbability += prize.probability;
                if (random <= cumulativeProbability) {
                    selectedPrize = prize;
                    break;
                }
            }

            document.getElementById('giftPicker').style.display = 'none';
            document.getElementById('giftReveal').style.display = 'block';
        }

        function revealGift() {
            document.getElementById('giftReveal').style.display = 'none';
            document.getElementById('giftResult').style.display = 'block';
            document.getElementById('prize').textContent = selectedPrize.name;
        }

        function reset() {
            document.getElementById('giftResult').style.display = 'none';
            document.getElementById('giftPicker').style.display = 'block';
        }
    </script>
</body>
</html>